-r requirements.txt

# S2DR4 runtime dependencies. Torch is installed first by scripts/setup_coderoom.sh
# so that a CUDA-enabled build is used when the CodeRoom has an NVIDIA GPU.
mgrs
utm
geojson
fiona
rasterio
geopy
h3
xmltodict
rioxarray
google-cloud
gspread
icecream
pystac-client
stackstac
einops
boto3
imagecodecs==2026.3.6

# Do not install arosics here. Its dependency resolver pulls the latest GDAL
# Python binding, which may not match the libgdal version in the CUDA image.
# Dockerfile/setup install arosics with --no-deps after the geospatial stack.
