-r requirements.txt

# S2DR4 runtime dependencies. Torch is installed first by Dockerfile/setup:
# CPU wheels by default for Cloud Run, CUDA wheels only when a GPU is present.
# Ubuntu's python3-gdal package is compiled against NumPy 1.x.
numpy<2
tqdm
mgrs
utm
geojson
fiona
rasterio
geopy
h3
xmltodict
rioxarray
google-cloud
gspread
icecream
pystac-client
stackstac
einops
boto3
imagecodecs==2024.12.30
cartopy
cmocean
folium!=0.12.0,>=0.6.0
joblib>=1.3.0
matplotlib
pandas
plotly
scikit-learn
scikit-image>=0.21.0
scipy>=1.7.0
dill
spectral

# Do not install py_tools_ds, geoarray, or arosics here. Their dependency
# resolvers pull GDAL from pip, which may not match the libgdal version in
# the CUDA image. Dockerfile/setup install them with --no-deps after the
# system GDAL Python binding is available.
